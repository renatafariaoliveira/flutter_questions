name: flutter_questions
description: A little project to study Flutter + Dart.
author: Renata Oliveira | renatafariaoliveira@gmail.com

version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.9.4

flutter:
  uses-material-design: true
  assets:
    - assets/images/